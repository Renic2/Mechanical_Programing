#include <iostream>
using namespace std;

void main(){ // cin이란? 사용자가 키보드에 입력한 값을 읽는 함수이다. <=> cout
    int a, b;
    cin >> a >> b; // cin은 공백 또는 엔터를 기준으로 값을 구분한다.
    cout << "a = " << a << "b= " << b << endl; 
}